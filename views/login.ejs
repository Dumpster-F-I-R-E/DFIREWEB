<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel='stylesheet' href='/stylesheets/login.css' />
   
</head>
<body>
    <div class="wrapper"> 
        <h1 class="title">Sign In</h1>  
       
        <div class="message">
            <% if (locals.message) { %>
                <%= message %>
            <% } %>              
            </div> 
        <form action="/login" method="POST">
                    <input class="row-1" type="text" name="username" id="username"  placeholder = "Username">
                    <input class="row-2" type="password" name="password" id="password" required placeholder = "Password">
                    <input class="btn" type="submit" value="Login">  
                    <input class="btn secondary-btn" type="button" value="Forgot Password">           
        </form>
    </div>
</body>
</html>