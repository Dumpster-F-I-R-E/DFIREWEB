/* eslint-disable no-undef */

const profileController = require('../controllers/mainMenuController');
const db = require('../database/db');
const init = require('../database/init');

const initializeDatabase = async () => {

};

const clearDatabase = async () => {
};

beforeAll(async () => {

});

afterAll(async () => {

});

beforeEach(async () => {
 
});

afterEach(async () => {
 
});

test('test getReports with existing user', async () => {

});